


      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">

            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" style="color:#005252">
            <ul class="nav navbar-nav">
              <li><a style="color:#005252" href="/users/signin">sign in</a></li>
              <li><a style="color:#005252" href="/users/new">register</a></li>
              <li><a style="color:#005252; padding-right:10px" href="/imprint">imprint</a></li>
            </ul>
          </div>
        </div>
      </nav>


      <div class="row">
        <div class="col-sm-8"></div>
        <div class="col-sm-4">

          <% if user %>       <!-- Abfrage, ob als User eingeloggt, dann Anzeige auf Startseite-->
          <p><%=user.email%><p>
            <form action='/sessions/delete' method='POST'>
              <input type="submit" value="log out"/>
            </form>
            <% else%>
            <% end %>

            <% if user %>
            <form action='/tasks/create' method='POST'>
              Do something: <input name="task" autofocus="true"/>
              <input type="submit" value="add to list"/>
            </form>


            <form method="GET" action="/">
              <button style="color:#550000" tabindex="15" name="alphabetically">A-Z</button>
              <button style="color:#550000" tabindex="15" name="newest">latest first</button>
            </form>


            <ul>
              <% tasks.each do |task| %>
              <li>
                <form action="/tasks/delete" METHOD="POST">
                  <%= h(task.name) %>
                  <input type="hidden" value="<%= task.id %>" name="task_id"/>
                  <button style="color:#880000" tabindex="15">delete</button>
                </form>
              </li>
              <% end %>
            </ul>
            <% end %>




